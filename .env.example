# URL of the Cloudflare Worker that proxies API keys server-side.
# For local development: cd worker && npm install && npm run dev
# Then set this to http://localhost:8787
VITE_WORKER_URL=http://localhost:8787
